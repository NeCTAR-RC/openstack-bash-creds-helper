#!/bin/bash
# shellcheck disable=SC1090

function chcreds() {
    local creds
    if ! creds=$(oscreds "$@"); then
        echo "Failed to load credentials" >&2
        return 1
    fi
    rmcreds
    source <(printf '%s\n' "$creds")
    echo "Credentials loaded for $OS_CRED"
}

function recred() {
    chcreds "$OS_CRED"
}

function rmcreds() {
    local v
    for v in $(env | grep '^OS_' | cut -d= -f1); do
        unset "$v"
    done
}

function prcreds() {
    local varname
    for varname in $(env | grep '^OS_' | cut -d= -f1); do
        if [[ "$varname" == *PASSWORD* || "$varname" == *TOKEN* || "$varname" == *SECRET* ]]; then
            printf '%s=******\n' "$varname"
        else
            printf '%s=%s\n' "$varname" "$(printenv "$varname")"
        fi
    done
}

function os_creds() {
    printf '%s' "${OS_CRED:+ $OS_CRED}"
}

# vim:syntax=sh
